# Job Hunter 推广视频脚本

**平台**：小红书 / 抖音  
**时长**：约 45 秒  
**制作方式**：混剪（口播 + 录屏画面 + 屏幕文字）  
**目标受众**：在校生 / 应届生 / 跨城求职的工程师  
**核心信息**：①这是干什么的 ②怎么用 ③GitHub 开源

---

## 脚本正文

| 时间 | 画面内容 | 口播文案 | 屏幕文字 |
|------|---------|---------|---------|
| **0-3s** | 黑屏淡入，快速闪过一张信息密集的招聘网页（如 ByteDance/TikTok careers 页面），然后用 "❌" 覆盖 | 还在对着几百个岗位一条条翻？ | 找工作 = 翻几百条 JD？ |
| **3-8s** | 画面切到终端：输入 `python3 run.py --url <招聘链接> --resume ./resume.pdf`，敲下回车 | 现在只要一行命令。输入招聘官网链接和你的简历，剩下的交给 Job Hunter。 | `python3 run.py --url <url> --resume ./resume.pdf` |
| **8-11s** | 终端滚动输出日志：Crawling → Collecting → Matching... （带进度感） | 它会自动抓取所有岗位，读取每一条 JD。 | ⚡ 自动抓取 + 读取详情 |
| **11-16s** | 画面切到流程图动效：Input（URL+简历） → Process（AI 匹配打分） → Output（PDF 手册）。打分柱状图或分数数字依次弹出 | 然后用 AI 跟你的简历做语义匹配，按匹配度排序——最合适的排最前面。 | 🤖 AI 匹配 → 按匹配度排序 |
| **16-22s** | 画面切到生成的 PDF 手册翻页效果：封面 → 岗位卡片 → 分数条 → "投递链接"按钮高亮 | 最终输出一份可以直接用的投递手册——岗位、匹配分数、一键链接，都在 PDF 里。 | 📄 生成「投递手册」PDF |
| **22-28s** | 快速展示三个场景：校招大量岗位筛选 / 不同城市对比 / 多家公司集中投递，用小标题切 | 校招海投、跨城市比 Offer、多个公司一起投，都适用。 | 🎯 校招 · 跨城 · 多公司 |
| **28-34s** | 画面扫过 GitHub 仓库页面，框出 README/Stars/许可证 | 项目已经在 GitHub 开源，MIT 协议，随便用。链接在评论区。 | ⭐ GitHub 开源 · MIT |
| **34-40s** | 回到终端画面，弹出 "Done!" 和 PDF 自动打开的提示。下方面板出现一句话总结 | 把翻几百页JD的时间，省下来做更重要的事吧。 | 把时间花在面试上，不是搜索上。 |

---

## 制作备注

### 画面素材清单
需要准备以下素材用于剪辑：

1. **终端操作录屏**（3-5 秒）
   - 在项目目录下输入 launch 命令并执行
   - 展示 terminal 输出有滚动感的画面

2. **流程图 / 动效画面**（可做成简单的 Keynote 动画或直接用 SVG）
   - 项目 `assets/flow-map.svg` 可以直接用
   - 建议在 Input → Process → Output 之间加箭头动画

3. **PDF 手册翻页录屏**（6-10 秒）
   - 用 `examples/public-demo.html` 或生成的 PDF
   - 录屏时慢速滚动，展示分数条和投递按钮

4. **GitHub 仓库页面截图或录屏**

5. **场景切换小标题**（后期加文本动画）

### 音乐建议
- **前 5 秒**：轻节奏鼓点，快速带入
- **6-28 秒**：中速电子/lo-fi beat，保持信息节奏
- **最后 5 秒**：收束感，留白让 CTA 落地

### 发布建议
- **标题**：一行命令，把招聘网站变成投递手册｜开源项目推荐
- **标签**：#开源 #求职 #效率工具 #程序员 #AI匹配
- **评论区置顶**：GitHub 地址 + 一句简短的说明（"项目叫 job-hunter，装好依赖一行命令就能跑"）
